import type { TOnboardingPageConfig } from './types';

export const DEFAULT_ONBOARDING_CONFIG: TOnboardingPageConfig = {
  ui: {
    title: 'ONBOARDING (prod)',
    description: 'description (prod)',
  },
  consts: {},
};
